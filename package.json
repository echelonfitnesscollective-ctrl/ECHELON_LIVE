{
  "private": true,
  "name": "echelon-live",
  "description": "Static storefront with Vercel serverless API routes (Stripe checkout, Supabase-backed forms).",
  "engines": {
    "node": ">=18"
  }
}
